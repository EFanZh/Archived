#ifndef PERMUTATION_H
#define PERMUTATION_H

// a: the array;
// b: array's size;
// return: 0 if a is the last permutation, otherwise 1.
int get_next_permutation(int *a, int n);

#endif // PERMUTATION_H
